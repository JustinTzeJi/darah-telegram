apiVersion: batch/v1
kind: Job
metadata:
  name: test-darah
spec:
  template:
    # This is the pod template
    spec:
      containers:
      - name: test-darah
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        command: ["python",
                  "test.py"]
      restartPolicy: Never
    # The pod template ends here
